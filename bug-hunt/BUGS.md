## باگ ۱ — [نمایش modal ابتدای لود شدن سایت]

- کجا: [./bug-hunt/public/style.css] خط 116 و 126
- مشکل: [class: modal-hidden کار نمیکرد]
- ریشه: [از آنجایی که modal دارای display:flex است،display:none را نمیپذیرد]
- fix: [اضافه کردن !important به diplay:none]

## باگ 2 — [رفرش شدن صفحه بعد از فشردن دکمه ی ذخیره در فرم]

- کجا: [./bug-hunt/public/app.js] خط 11
- مشکل: [رفرش شدن صفحه بعد از فشردن دکمه ی ذخیره در فرم]
- ریشه: [رفتار طبیعی تگ فرم که بعد از سابمیت شدن صفحه را رفرش میکند]
- fix: [اضافه کردن event.preventDefault() برای جلوگیری از رفتار دیفالت فرم]

## باگ 3 — [عدم نمایش لیست کتاب ها در رابط کاربری]

- کجا: [./bug-hunt/public/app.js] خط 7
- مشکل: [عدم نمایش لیست کتاب ها در رابط کاربری]
- ریشه: [ساختار غلط در استفاده از async/await. زمانی که از یک تابع ناهمزمان استفاده میکنیم مقداری که متغیر book به ما تحویل میدهد یک promise است نه یک دیتا،از این رو برای اینکه دستورات بعدی مبتنی بر داده ی ناهمزمان باشد باید منتظر بمانیم تا دیتا بصورت کامل دریافت شود]
- fix: [اضافه کردن Await و trycatch block برای مدیریت خطاها]

## باگ 4 — [عدم حذف کتاب از db.json با فشردن دکمه ی حذف]

- کجا: [./bug-hunt/public/render.js] خط 18 , 36
- مشکل: [عدم حذف کتاب از db.json با فشردن دکمه ی حذف]
- ریشه: [با فشردن تابع deleteBook تابع اصلا آیدی دریافت نمیکند که بخواهد کتاب را حذف کند]
- fix: [به المنت card آیدی اضافه شد و با فشردن دکمه ی حذف این آیدی به عنوان ورودی تابع deleteBook قرار میگیرد]

## باگ 5 — [refactoring async/await functions & error handling]

- کجا: [./bug-hunt/public/api.js , ./bug-hunt/public/app.js]
- مشکل: [ساختار نادرست تابع های ناهمزمان و عدم مدیریت خطاها]
- ریشه: [زمانی که در یک تابع Async از await استفاده نمیکنیم تابع یک promise را برمیگرداند در حالی که باید با await منتظر باشیم دیتا بطور کامل لود شده و سپس با تابع jdon() دیتا هارا دریافت کنیم.همچنین نبود trycatch block باعث میشود به غیر از خطا های اینترنت باقی خطاها catch نشده و بدون هیچ الرتی عملکرد برنامه را مختل کند]
- fix: [افزودن trycatch block به فایل های یاد شده]
